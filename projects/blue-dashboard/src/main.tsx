import React from 'react'
import ReactDOM from 'react-dom/client'
import { Box } from '../../green-adbox/src/Box'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Box />
  </React.StrictMode>
)
